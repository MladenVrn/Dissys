import java.util.ArrayList;
import java.util.HashSet;
import java.util.Iterator;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        /*ArrayList<Publication> l = new ArrayList<>();
        Publication p = new Publication("Brida","German",99.99);
        Book b = new Book("Coelho",111111,"Alchemist","german",99.00);

        l.add(p);
        l.add(b);

        for(Publication pl : l){
                pl.print();
            }*/

       /* //Scanner
        Scanner sc = new Scanner(System.in);
        String line = sc.nextLine();
        //store scanner value in HashSet
        HashSet<String> hl = new HashSet<>();
        hl.add(line);
        //create iterator
        Iterator<String> iter = hl.iterator();
        //print values from HashSet using Iterator
        while(iter.hasNext()){
            System.out.println(iter.next().toString());
        }*/



    }
}